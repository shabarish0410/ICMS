[supervisord]
nodaemon=true

[program:backend]
directory=/app/backend
command=uvicorn app.main:app --host 0.0.0.0 --port 8000
autorestart=true

[program:frontend]
directory=/app/.frontend
command=npm start
autorestart=true
